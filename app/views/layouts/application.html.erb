<!DOCTYPE html>
<html>
  <head>
    <title>Payment</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/dashboard_navbar' %>
    <nav>
      <%= link_to 'Registrar como usuário', new_user_registration_path %>
      <%= link_to 'Logar como usuário', new_user_session_path %>
      <%= link_to 'Registrar como administrador', new_admin_registration_path %>
      <%= link_to 'Logar como administrador', new_admin_session_path %>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
